// Middleware temporarily disabled to debug auth issues
// TODO: re-enable after confirming app works with OAuth

export default function middleware() {
  // pass through all requests
}

export const config = {
  matcher: [],
};
